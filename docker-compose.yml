version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./backend:/app/backend
    environment:
      - FLASK_APP=backend/app.py
      - FLASK_ENV=development
    env_file:
      - .env
